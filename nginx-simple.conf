# Configuration Nginx simple pour HestiaCP + Laravel

# Point d'entrée Laravel
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

# Sécurité de base
location ~ /\.env {
    deny all;
}

location ~ /\.git {
    deny all;
}